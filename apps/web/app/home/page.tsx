import { HomeHeader } from '@/src/widgets';
import React from 'react';

type Props = {};

const page = (props: Props) => {
  return (
    <div className="w-full px-16 py-12">
      <HomeHeader />
    </div>
  );
};

export default page;
